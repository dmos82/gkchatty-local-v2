[build]
  base = "frontend"
  command = "cd .. && pnpm install --filter @gkchatty/web && cd frontend && pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
