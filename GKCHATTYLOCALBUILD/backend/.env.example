# GKChatty API Environment Variables Example
# Copy this file to .env in the apps/api directory and fill in your actual values.
# Do NOT commit your actual .env file to Git.

# --- Authentication ---
# Generate a strong random string (e.g., 32+ characters)
JWT_SECRET=<your_strong_random_jwt_secret_here>

# --- Database ---
# Your MongoDB Atlas connection string
MONGODB_URI=<your_mongodb_atlas_connection_string_here>

# --- Pinecone Vector Database ---
PINECONE_API_KEY=<your_pinecone_api_key_here>
PINECONE_ENVIRONMENT=<your_pinecone_environment_here_e.g_us-east-1>
# Default Pinecone index name for development environments
PINECONE_INDEX_NAME=gkchatty-dev

# --- OpenAI API ---
OPENAI_API_KEY=<your_openai_api_key_here>
# Optional: If you use a separate key for embeddings (usually not needed)
# OPENAI_EMBEDDING_API_KEY=<your_openai_embedding_api_key_here>

# --- OpenAI Model Configuration ---
# Primary chat model (e.g., gpt-4o-mini, gpt-3.5-turbo)
# Defaults to gpt-4o-mini if unset in code.
OPENAI_PRIMARY_CHAT_MODEL=gpt-4o-mini
# Fallback chat model if primary fails or for specific tasks
# Defaults to gpt-3.5-turbo if unset in code.
OPENAI_FALLBACK_CHAT_MODEL=gpt-3.5-turbo
# Embedding model
# Defaults to text-embedding-ada-002 if unset in code.
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002

# --- File Storage ---
# For local development, files are stored locally. For production, use S3.
# Options: 'S3' or 'LOCAL' (defaults to LOCAL if AWS_BUCKET_NAME is 'local' or not S3)
FILE_STORAGE_MODE=LOCAL
# If FILE_STORAGE_MODE is S3:
AWS_REGION=<your_aws_s3_bucket_region_e.g_us-east-1>
AWS_ACCESS_KEY_ID=<your_aws_access_key_id>
AWS_SECRET_ACCESS_KEY=<your_aws_secret_access_key>
AWS_BUCKET_NAME=<your_aws_s3_bucket_name>
# If FILE_STORAGE_MODE is LOCAL (or defaulting to local):
# Path for local file uploads, relative to apps/api directory or absolute.
# Example: .local_uploads (will create a .local_uploads folder inside apps/api)
LOCAL_FILE_STORAGE_DIR=.local_uploads

# --- Server Configuration ---
# Port the API server will listen on
PORT=6001

# --- Security & CORS ---
# Comma-separated list of allowed origins for CORS (e.g., http://localhost:6004,https://yourapp.com)
# If empty or not set, some default restrictive behavior might apply or be logged.
# For development, you might use: ALLOWED_ORIGINS=http://localhost:6004
ALLOWED_ORIGINS=http://localhost:6004

# --- Other Optional Settings ---
# Example: If you were using ChromaDB (currently seems secondary)
# CHROMA_URL=http://localhost:8000

# ENCRYPTION_KEY (if used for specific data encryption, ensure it's a strong random key)
# ENCRYPTION_KEY=<your_32_byte_encryption_key_hex_encoded>