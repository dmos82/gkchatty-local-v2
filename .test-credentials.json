{
  "$schema": "./test-credentials.schema.json",
  "description": "Centralized test credentials - SINGLE SOURCE OF TRUTH",
  "version": "1.0.0",
  "credentials": {
    "dev": {
      "username": "dev",
      "password": "dev123",
      "role": "admin",
      "description": "Primary development test user - used by stress tests, MCPs, and E2E tests",
      "createdBy": "backend seeding (src/index.ts)",
      "usedBy": [
        "scripts/stress-test.sh",
        "MCP environment variables",
        "E2E tests",
        "Manual testing"
      ]
    },
    "admin": {
      "username": "admin",
      "password": "admin123",
      "role": "admin",
      "description": "Alternative admin user for testing admin-specific features"
    },
    "initial_admin": {
      "username": "initial_admin",
      "password": "changeme",
      "email": "admin@example.com",
      "role": "admin",
      "description": "Auto-seeded admin user - created on first startup"
    }
  },
  "notes": {
    "security": "These are TEST ONLY credentials. NEVER use in production.",
    "updating": "When changing credentials, update ALL references listed in 'usedBy'",
    "verification": "Run ./scripts/verify-credentials.sh to check consistency"
  }
}
